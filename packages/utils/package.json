{
  "name": "@dzs/utils",
  "version": "1.0.0",
  "description": "DZS-OS 工具函数层 — 通用工具方法",
  "main": "src/index.ts",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
